% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doe_wrapper.R
\name{plot.doe_results}
\alias{plot.doe_results}
\title{Plot DOE metrics for a single linear trajectory}
\usage{
\method{plot}{doe_results}(doe_results, type = c("bar", "radar", "heatmap"))
}
\arguments{
\item{doe_results}{A result object returned by
\code{\link[=compute_single_DOE_linear]{compute_single_DOE_linear()}}, typically containing elements
\verb{$D}, \verb{$O}, \verb{$E}, and \verb{$DOE_score}.}

\item{type}{Character string indicating plot type:
one of \code{"bar"}, \code{"radar"}, or \code{"heatmap"}.}
}
\value{
\itemize{
\item For \code{"bar"} or \code{"heatmap"}: a \pkg{ggplot2} object.
\item For \code{"radar"}: draws the plot and invisibly returns \code{NULL}.
}
}
\description{
Visualize the Directionality (D), Order (O), and Endpoint (E)
component metrics — along with the combined DOE score —
from a single trajectory analysis.
}
\details{
This function summarizes all DOE components into an easy-to-read visual.
Each metric is normalized between 0 and 1 where applicable.
Missing (\code{NA}) values are handled gracefully.
}
\examples{
\dontrun{
res <- compute_single_DOE_linear(expr, pt, naive, term)
plot.doe_results(res, type = "bar")
plot.doe_results(res, type = "radar")
plot.doe_results(res, type = "heatmap")
}

}
